import { ScrollView } from 'react-native'
import Header from '~/components/Header'

const LecturerView = () => {
    return (
        <ScrollView>
            <Header />
        </ScrollView>
    )
}

export default LecturerView
