import { ScrollView } from 'react-native'
import Header from '~/components/Header'

const LecturesView = () => {
    return (
        <ScrollView>
            <Header />
        </ScrollView>
    )
}

export default LecturesView
