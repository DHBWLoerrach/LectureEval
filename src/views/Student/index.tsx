import { ScrollView } from 'react-native'
import Header from '~/components/Header'

const StudentView = () => {
    return (
        <ScrollView>
            <Header />
        </ScrollView>
    )
}

export default StudentView
